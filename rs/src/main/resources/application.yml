server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book_exchange?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 文件上传配置
file:
  upload-path: /Users/weibaiwang/Desktop/AAA/work/all/rs/src/main/resources/static/uploads/
  access-path: /uploads/

# JWT配置
jwt:
  secret: bookExchangeSecretKey2024
  expiration: 86400000

# 微信小程序配置(模拟)
wx:
  appid: wx_mock_appid
  secret: wx_mock_secret
